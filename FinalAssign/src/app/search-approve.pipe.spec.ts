import { SearchApprovePipe } from './search-approve.pipe';

describe('SearchApprovePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchApprovePipe();
    expect(pipe).toBeTruthy();
  });
});
